// `$localize` for the editor and ESLint, whose program starts from the root tsconfig; the build gets it from
// tsconfig.app.json. Translations are loaded at runtime in main.ts.
/// <reference types="@angular/localize" />
